extends Area2D

onready var ASDASDASD = get_parent()

# Add this obstacle to the navigation mesh
#func _ready():
#	yield(get_tree().create_timer(2), "timeout")
#	yield(get_tree(), "idle_frame")
#	ASDASDASD.set_obstacle(self)
